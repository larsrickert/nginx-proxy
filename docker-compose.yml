version: "3"

services:
  docs:
    build: .
    image: larsrickert/nginx-proxy-docs
    restart: always
    ports:
      - "80:80"
    environment:
      VIRTUAL_HOST: "${DOMAIN}"
      LETSENCRYPT_HOST: "${DOMAIN}"
